{
  "name": "crit-2048",
  "version": "1.0.0",
  "description": "A roguelike dungeon crawler built on 2048, inspired by Balatro and D&D 5e",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:android": "tauri android dev",
    "build:desktop": "tauri build",
    "build:android": "tauri android build",
    "build:all": "npm run build:desktop && npm run build:android"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
